import Popup from './popup';
import '../../../index.scss';

export default {
  title: 'Popup',
  component: Popup,
  tags: ['autodocs'],
};

export const Default = {
  args: {
    children: '',
  },
};
